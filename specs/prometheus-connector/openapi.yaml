openapi: "3.1.0"
info:
  title: Prometheus Connector API
  version: "0.1.0"
  description: >
    Queries Azure Monitor Workspace (Prometheus API) for CPU, memory,
    quota, OOM, and Cilium flow metrics across all environments.
paths: {}
